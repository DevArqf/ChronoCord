module.exports = {
  // Default embed styling
  defaultColor: '#3498db',        // primary default for polls
  notifyColor: '#0cf400ff',         // color for notify / success embeds
  listColor: '#00b0f4',           // color for listing events

  // Default footer / author (can be strings or null)
  defaultFooterText: 'ChronoCord | Alpha',
  defaultAuthorName: null,
  defaultAuthorIcon: null,

  // Other useful defaults
  maxSelectOptions: 25,
};